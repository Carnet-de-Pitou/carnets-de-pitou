(()=>{const nativeFetch=window.fetch.bind(window),target="Ma Croisade.. ~~ Prophecy, Ultima Online";function dec(s){const bin=atob(s.replace(/\s/g,''));return new TextDecoder().decode(Uint8Array.from(bin,c=>c.charCodeAt(0)))}function enc(s){const bytes=new TextEncoder().encode(s);let bin='';for(const b of bytes)bin+=String.fromCharCode(b);return btoa(bin)}window.fetch=async function(input,init){try{const url=String(input);if(init?.method==='PUT'&&url.includes('/contents/library.js')){const body=JSON.parse(init.body),src=dec(body.content),marker='window.PITOU_PUBLIC_LIBRARY = ',i=src.indexOf(marker);if(i>=0){let raw=src.slice(i+marker.length).trim();if(raw.endsWith(';'))raw=raw.slice(0,-1);const list=JSON.parse(raw),slug=document.getElementById('edOriginalSlug')?.value||((document.getElementById('edTitle')?.value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'texte'),cat=document.getElementById('edCategory')?.value||'',series=document.getElementById('edSeries')?.value||'',item=list.find(t=>t&&t.slug===slug);if(item){if(cat===target&&series)item.series=series;else delete item.series}body.content=enc(marker+JSON.stringify(list)+';\n');init={...init,body:JSON.stringify(body)}}}}catch(e){console.warn('Sous-section non injectée',e)}return nativeFetch(input,init)}})();